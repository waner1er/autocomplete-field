{{ form_start(form) }}
<div>
<div>
    {{form_label(form.nom) }}
    {{ form_widget(form.nom) }}
</div>
    <br>
<div>
    {{ form_errors(form.nom) }}
    {{ form_widget(form.address) }}
</div>
</div>
{{ form_end(form) }}
